# Handoff - 2026-02-21

## Completed

- SensorList widget is registered natively with Ethos (`system.registerWidget`).
- Sensor discovery now works in simulator and reports expected IDs for the tested model.
- Build flow for Ethos-installable ZIP is in place (`tools/build-package.ps1`).
- Manual list scrolling via wheel/button events is implemented.
- Callback nil guards were added in `paint` and `wakeup` paths to prevent startup/navigation crashes.

## Current Behavior

- List is static by default (no auto-scroll loop).
- User can scroll up/down via mapped input events.
- Data refresh is low frequency to avoid instruction-limit issues.

## Known Risks / Follow-up

- Input event constants can vary by firmware/build; verify scroll events on physical radio.
- If scrolling does not work on hardware, add temporary event-code debug overlay.
- Consider optional page indicator (`offset/total`) in footer for usability.
- Consider option to hide debug empty-state lines once hardware behavior is fully validated.

## Suggested Next Session Tasks

1. Validate wheel scrolling on physical X20RS and adjust event mapping if needed.
2. Add visual page position indicator.
3. Add widget options (for example: refresh interval, compact mode, debug mode).
4. Replace `tools/deploy-ethos-sim.ps1` placeholder with real simulator deployment copy logic.
